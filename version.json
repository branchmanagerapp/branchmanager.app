{
  "version": 1044,
  "released": "2026-07-13",
  "notes": "Stale-version banner: if your device can't auto-update to the latest code (usually a stuck service worker), a loud 'You're on an older version — Update now' banner appears with a one-tap hard reset (clears the service worker + caches). No more silently running old code and hitting already-fixed bugs."
}
