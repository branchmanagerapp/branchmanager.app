{
  "version": 1115,
  "released": "2026-08-12",
  "notes": "v1115 AUTO-PIPELINE hardening: quoteToJob no longer writes phantom 'source' col (cloud jobs has none) — on a fresh device the auto-convert sweep could create a job before the column set is learned, 400ing the push so the job never reached cloud (the Catherine-can't-see-it failure mode); quote linkage kept via quote_id"
}
