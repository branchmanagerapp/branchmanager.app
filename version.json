{
  "version": 1055,
  "released": "2026-07-14",
  "notes": "Root-cause fix for quotes vanishing from the app: quotes is the biggest cached table (~1.6MB) and would silently blow the phone's localStorage cap and fail to cache. Now degrades gracefully — active quotes always stay on the phone, done ones load from the cloud — instead of dropping the whole table."
}
