{
  "version": 838,
  "released": "2026-05-19",
  "notes": "in-app new-request notifier: realtime postgres_changes INSERT on requests fires browser notification (if permitted) + toast + beep + live-refresh requests/pipeline page if open. Click notification opens BM → Requests. Tenant-scoped via RLS. Mirrors the existing inbound-comm notifier."
}
