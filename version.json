{
  "version": 982,
  "released": "2026-06-28",
  "notes": "v982 (perf): parallelize cold-load cloud pull — _pullFromCloud now fetches all tables concurrently (Promise.all) instead of sequentially, so cold-load wall-clock drops from sum-of-tables to slowest-single-table. Each table writes its own localStorage key (no race); per-table error handling unchanged."
}
