{
  "version": 943,
  "released": "2026-06-14",
  "notes": "Safety guard: never auto-boot into the Books page on cold load (it currently hangs the renderer), so a reopen can't get trapped on a dead page. Falls back to Dashboard + clears saved route. In-app Books click still works. Temporary until the Books render hang is fixed. No other changes; bundle unchanged (bm.bundle.v941.min.js)."
}
