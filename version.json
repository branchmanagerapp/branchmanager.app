{
  "version": 1039,
  "released": "2026-07-13",
  "notes": "Quote line items: (1) add a service by TYPING it (type-or-pick input + Add button) instead of only picking from a dropdown; (2) the line-item delete is now a clearly-labeled '🗑 Delete line item' button (was a bare ✕ that read like close/collapse), with a confirm to prevent accidental deletion."
}
