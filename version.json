{
  "version": 1021,
  "released": "2026-07-12",
  "notes": "Force-update hardened: stale devices now hard-refresh via a cache-busting URL (fixes the iOS case where location.reload() served the cached index.html and the app never updated), with a loop guard. This is what kills the recurring stale-cache 400s."
}
