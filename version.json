{
  "version": 1128,
  "released": "2026-08-15",
  "notes": "THE drag-revert fix: cloud writes now use the authed session token (RLS was silently zero-rowing every anon PATCH since Jul 8); zero-row writes now trip the loud banner + beacon"
}