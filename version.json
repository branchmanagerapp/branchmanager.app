{
  "version": 1064,
  "released": "2026-07-15",
  "notes": "Tenant switch now sticks: switchTo calls set_active_tenant RPC + refreshes the session so the JWT re-stamps; caches wiped on switch. (Server: is_active-aware token hook, SECURITY DEFINER restored.)"
}