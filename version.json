{
  "version": 1041,
  "released": "2026-07-13",
  "notes": "Clients search fixed: removed the box-in-box (borderless input inside the pill), and debounced so it no longer re-renders + steals focus on every keystroke — you can type a full query uninterrupted, so filtering works (typing 'Rob' shows the Robs, not unfiltered A's)."
}
